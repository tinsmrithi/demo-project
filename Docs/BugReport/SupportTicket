================================================================================
Support Ticket #4821
================================================================================
Date received: 2024-01-16 14:31:07 UTC
Channel: Customer Portal
Status: Open — awaiting QA triage
Priority: (unset)
Plan: Enterprise
Account: Acme Enterprise (acme-enterprise.com)
Contact: j.morrison@acme-enterprise.com
Employees in system: ~4,200

--------------------------------------------------------------------------------
Subject: export doesn't work

Hi,

The export doesn't work. We need this for payroll so it's urgent.

Please fix asap.

Thanks
J Morrison
Head of HR Operations
Acme Enterprise

--------------------------------------------------------------------------------
Attachment: screenshot.png
  [blurry mobile screenshot showing loading spinner on the Reports page]
  No error message visible. Spinner appears to be indefinitely loading.

--------------------------------------------------------------------------------
Support agent notes (added 14:47):
  Attempted to call customer — no answer. Left voicemail.
  Could not reproduce in sandbox (small dataset).
  Escalating to QA for investigation.

================================================================================
CONTEXT FOR QA INVESTIGATION
================================================================================

Recent changes to the export feature:
  - v2.3 shipped 2024-01-10 (6 days ago)
  - Changed: export now uses async queue system instead of synchronous generation
  - Changed: PDF export now runs through a new rendering service
  - No changes to CSV export logic (or so the PR claimed)

Related Jira tickets:
  - TS-891: Export async queue migration (merged to main 2024-01-08)
  - TS-902: PDF rendering service integration (merged 2024-01-09)
  - TS-908: Export UI progress indicator (merged 2024-01-10)

Known constraints:
  - Enterprise accounts can have up to 50,000 employee records
  - Acme has ~4,200 employees — one of our larger accounts
  - The async queue has a 30-second job timeout (introduced in v2.3)
  - The old synchronous export had a 60-second timeout

================================================================================